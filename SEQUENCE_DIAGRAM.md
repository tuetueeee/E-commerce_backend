# 🔄 Sequence Diagrams - API Flow Visualization

Các diagram thể hiện trình tự các API calls và interactions trong hệ thống.

---

## 1️⃣ USER REGISTRATION & LOGIN FLOW

```
┌─────────────┐                                           ┌─────────────┐
│   CLIENT    │                                           │   SERVER    │
└──────┬──────┘                                           └──────┬──────┘
       │                                                         │
       │  POST /api/auth/register                              │
       │  { name, email, password, phone, address }           │
       ├────────────────────────────────────────────────────>│
       │                                                    │ Create User
       │                                                    │ Hash Password
       │                                                    │ Generate Token
       │  201 Created                                       │
       │  { user, token }                                   │
       │<────────────────────────────────────────────────────┤
       │                                                         │
       ├─ Store token in localStorage                           │
       │                                                         │
       │  Next visit - Auto login with token                    │
       │  GET /api/auth/profile                                 │
       │  Authorization: Bearer {token}                         │
       ├────────────────────────────────────────────────────>│
       │                                                    │ Verify Token
       │  200 OK                                           │
       │  { user profile }                                 │
       │<────────────────────────────────────────────────────┤
       │                                                         │
```

---

## 2️⃣ PRODUCT BROWSING FLOW

```
┌─────────────┐                                           ┌─────────────┐
│   CLIENT    │                                           │   SERVER    │
└──────┬──────┘                                           └──────┬──────┘
       │                                                         │
       │  1. GET /api/categories                                │
       │     (Show category list)                              │
       ├────────────────────────────────────────────────────>│
       │  { categories }                                    │
       │<────────────────────────────────────────────────────┤
       │                                                         │
       │  2. GET /api/categories/:id/products                   │
       │     (Show products in category)                       │
       ├────────────────────────────────────────────────────>│
       │  { products with pagination }                     │
       │<────────────────────────────────────────────────────┤
       │                                                         │
       │  3. GET /api/products/:id                              │
       │     (Show product details)                            │
       ├────────────────────────────────────────────────────>│
       │  { full product data                               │
       │     - SKU variants                                    │
       │     - Reviews & ratings                               │
       │     - Images                                          │
       │     - Related products }                              │
       │<────────────────────────────────────────────────────┤
       │                                                         │
       │  4. Optional: AI Recommendations                       │
       │     GET /api/products/ai/similar/:id                  │
       │     GET /api/products/ai/frequently-bought/:id        │
       ├────────────────────────────────────────────────────>│
       │  { recommended products }                         │
       │<────────────────────────────────────────────────────┤
       │                                                         │
```

---

## 3️⃣ CUSTOMIZATION & PRICING FLOW

```
┌─────────────┐                                           ┌─────────────┐
│   CLIENT    │                                           │   SERVER    │
└──────┬──────┘                                           └──────┬──────┘
       │                                                         │
       │  1. POST /api/customizer/calculate-price              │
       │     {                                                  │
       │       productId, colorCode, sizeCode,                 │
       │       customDesignData                                │
       │     }                                                  │
       ├────────────────────────────────────────────────────>│
       │                                                    │ Calculate:
       │                                                    │ - Base Price
       │                                                    │ - Custom Fee
       │                                                    │ - Print Fee
       │                                                    │ - Materials
       │  { totalPrice, breakdown }                        │
       │<────────────────────────────────────────────────────┤
       │                                                         │
       │  2. Optional: Save Design                              │
       │     POST /api/customizer/save                         │
       │     { name, productId, elements, ... }               │
       ├────────────────────────────────────────────────────>│
       │  { savedDesign }                                  │
       │<────────────────────────────────────────────────────┤
       │                                                         │
       │  3. Later: Load Saved Design                           │
       │     GET /api/customizer/saved/:designId               │
       ├────────────────────────────────────────────────────>│
       │  { design with all data }                         │
       │<────────────────────────────────────────────────────┤
       │                                                         │
```

---

## 4️⃣ SHOPPING CART FLOW

```
┌─────────────┐                                           ┌─────────────┐
│   CLIENT    │                                           │   SERVER    │
└──────┬──────┘                                           └──────┬──────┘
       │                                                         │
       │  1. POST /api/cart/add                                 │
       │     {                                                  │
       │       productId, quantity,                             │
       │       colorCode, sizeCode,                             │
       │       customDesignData                                 │
       │     }                                                  │
       ├────────────────────────────────────────────────────>│
       │                                                    │ Create CartItem
       │                                                    │ Calculate Subtotal
       │  { cartItem }                                     │
       │<────────────────────────────────────────────────────┤
       │                                                         │
       │  2. Can repeat: Add More Items                         │
       │     POST /api/cart/add  (different products)          │
       ├────────────────────────────────────────────────────>│
       │<────────────────────────────────────────────────────┤
       │                                                         │
       │  3. View Cart                                          │
       │     GET /api/cart                                     │
       ├────────────────────────────────────────────────────>│
       │  { cartId, items, totalAmount }                   │
       │<────────────────────────────────────────────────────┤
       │                                                         │
       │  4. Optional: Update Quantities                        │
       │     PATCH /api/cart/items/:itemId                     │
       │     { quantity: 3 }                                   │
       ├────────────────────────────────────────────────────>│
       │  { updatedItem }                                  │
       │<────────────────────────────────────────────────────┤
       │                                                         │
       │  5. Optional: Apply Voucher                            │
       │     POST /api/cart/apply-voucher                      │
       │     { voucherCode: "SAVE20" }                         │
       ├────────────────────────────────────────────────────>│
       │                                                    │ Validate Voucher
       │                                                    │ Calculate Discount
       │  { discountAmount, newTotal }                     │
       │<────────────────────────────────────────────────────┤
       │                                                         │
       │  Ready for Checkout →                                  │
       │                                                         │
```

---

## 5️⃣ CHECKOUT & ORDER CREATION FLOW

```
┌─────────────┐                                           ┌─────────────┐
│   CLIENT    │                                           │   SERVER    │
└──────┬──────┘                                           └──────┬──────┘
       │                                                         │
       │  1. Select Shipping Address                            │
       │     GET /api/addresses                                │
       ├────────────────────────────────────────────────────>│
       │  { addresses list }                               │
       │<────────────────────────────────────────────────────┤
       │                                                         │
       │  2. Or Add New Address                                 │
       │     POST /api/addresses                               │
       │     { street, city, country, ... }                   │
       ├────────────────────────────────────────────────────>│
       │  { newAddress }                                   │
       │<────────────────────────────────────────────────────┤
       │                                                         │
       │  3. Select Payment Method                              │
       │     GET /api/payment-methods                          │
       ├────────────────────────────────────────────────────>│
       │  { paymentMethods }                               │
       │<────────────────────────────────────────────────────┤
       │                                                         │
       │  4. Review Cart & Create Order                         │
       │     POST /api/orders                                  │
       │     {                                                  │
       │       items: [{ cartItemId, productId, qty, ... }],   │
       │       shippingAddressId,                               │
       │       paymentMethodId,                                 │
       │       voucherCode                                      │
       │     }                                                  │
       ├────────────────────────────────────────────────────>│
       │                                                    │ Validate:
       │                                                    │ - Stock
       │                                                    │ - Address
       │                                                    │ - Voucher
       │                                                    │ Create Order
       │                                                    │ Clear Cart
       │  201 Created                                       │
       │  { orderId, Status: PENDING }                     │
       │<────────────────────────────────────────────────────┤
       │                                                         │
       │  5. Proceed to Payment                                 │
       │     (See Payment Flow below)                           │
       │                                                         │
```

---

## 6️⃣ PAYMENT FLOW (VNPay)

```
┌─────────────┐              ┌─────────────┐              ┌─────────────┐
│   CLIENT    │              │   SERVER    │              │   VNPay     │
└──────┬──────┘              └──────┬──────┘              └──────┬──────┘
       │                            │                             │
       │  1. Initiate Payment       │                             │
       │  POST /api/payments/initiate
       │  { orderId, amount }       │                             │
       ├──────────────────────────>│                             │
       │                        │ Create Payment Record     │
       │                        │ Generate VNPay URL        │
       │  { paymentUrl }        │                             │
       │<──────────────────────────┤                             │
       │                            │                             │
       │  2. Redirect to VNPay      │                             │
       │                            │                             │
       │  https://vnpay.vn/pay?...  │                             │
       │─────────────────────────────────────────────────────────>│
       │                            │                             │
       │  3. User Enters Card Details                             │
       │  (Happens in VNPay)        │                             │
       │                            │                             │
       │  4. VNPay Processes Payment│                             │
       │                            │                             │
       │  5. Callback to Server     │                             │
       │  GET /api/payments/callback/vnpay?...                    │
       │                            │<─────────────────────────────┤
       │                            │ Validate Signature
       │                            │ Update Payment Status
       │                            │ Update Order Status
       │                            │
       │  6. Return Success Page    │                             │
       │<─────────────────────────────────────────────────────────┤
       │                            │                             │
       │  7. Optional: Verify Payment (backend check)             │
       │  POST /api/payments/:id/verify                           │
       ├──────────────────────────>│                             │
       │  { success: true }     │                             │
       │<──────────────────────────┤                             │
       │                            │                             │
       │  8. Get Order Status       │                             │
       │  GET /api/orders/:orderId  │                             │
       ├──────────────────────────>│                             │
       │  { Order, Status: CONFIRMED, paymentStatus: PAID }  │
       │<──────────────────────────┤                             │
       │                            │                             │
```

---

## 7️⃣ ORDER PROCESSING & SHIPMENT FLOW

```
┌──────────────┐     ┌──────────────┐     ┌──────────────┐     ┌──────────────┐
│   CUSTOMER   │     │   SELLER     │     │   SERVER     │     │  SHIPPING    │
│   (Client)   │     │  (Admin)     │     │   (Backend)  │     │   PROVIDER   │
└──────┬───────┘     └──────┬───────┘     └──────┬───────┘     └──────┬───────┘
       │                    │                    │                    │
       │  Order placed      │                    │                    │
       │  Status: PENDING   │                    │                    │
       │<─────────────────────────────────────────│                    │
       │                    │                    │                    │
       │                    │ Admin reviews order│                    │
       │                    │                    │                    │
       │                    │ PATCH /api/orders/:id/status            │
       │                    │ { status: CONFIRMED }                   │
       │                    ├───────────────────>│                    │
       │                    │                    │ Order prepared     │
       │                    │                    │ Pick items         │
       │                    │                    │ Pack order         │
       │                    │                    │                    │
       │  Notified about    │                    │                    │
       │  shipment          │                    │ Create Shipment    │
       │<──────────────────────────────────────────│                    │
       │                    │                    │ Assign Tracking #  │
       │                    │                    │                    │
       │                    │                    │ Send to Shipping   │
       │                    │                    │ Provider           │
       │                    │                    ├───────────────────>│
       │                    │                    │                    │ Pick up
       │                    │                    │                    │ In transit
       │                    │                    │<───────────────────┤
       │ Check tracking     │                    │ Update tracking    │
       │ GET /api/orders/:id/tracking            │                    │
       ├────────────────────────────────────────>│                    │
       │  { shipment with tracking events }  │                    │
       │<────────────────────────────────────────┤                    │
       │                    │                    │                    │
       │  Package arrives   │                    │                    │
       │  Confirmed delivery│                    │ Delivered          │
       │<──────────────────────────────────────────│<────────────────────┤
       │                    │                    │                    │
       │                    │ PATCH /api/orders/:id/status            │
       │                    │ { status: DELIVERED }                   │
       │                    ├───────────────────>│                    │
       │                    │                    │ Earn rewards       │
       │                    │                    │ Can leave review   │
       │  Can leave review  │                    │                    │
       │  & rate product    │                    │                    │
       │  POST /api/reviews │                    │                    │
       ├────────────────────────────────────────>│                    │
       │  { review created }│                    │                    │
       │<────────────────────────────────────────┤                    │
       │                    │                    │                    │
```

---

## 8️⃣ REVIEW & RATINGS FLOW

```
┌─────────────┐                                           ┌─────────────┐
│   CLIENT    │                                           │   SERVER    │
└──────┬──────┘                                           └──────┬──────┘
       │                                                         │
       │  1. After delivery: View Product                        │
       │     GET /api/products/:id                              │
       ├────────────────────────────────────────────────────>│
       │  { product details + existing reviews }          │
       │<────────────────────────────────────────────────────┤
       │                                                         │
       │  2. User writes & submits review                        │
       │     POST /api/reviews                                  │
       │     {                                                  │
       │       productId,                                       │
       │       rating: 5,                                       │
       │       comment: "Great product!",                       │
       │       images: ["..."]                                  │
       │     }                                                  │
       ├────────────────────────────────────────────────────>│
       │                                                    │ Verify purchase
       │                                                    │ Create review
       │                                                    │ Recalculate rating
       │  201 Created                                       │
       │  { reviewId, rating, comment }                    │
       │<────────────────────────────────────────────────────┤
       │                                                         │
       │  3. Get updated product reviews                         │
       │     GET /api/reviews/product/:id                       │
       ├────────────────────────────────────────────────────>│
       │  { reviews list + new average rating }           │
       │<────────────────────────────────────────────────────┤
       │                                                         │
       │  4. Other users can see the review                      │
       │                                                         │
```

---

## 9️⃣ REWARD & REDEMPTION FLOW

```
┌─────────────┐                                           ┌─────────────┐
│   CLIENT    │                                           │   SERVER    │
└──────┬──────┘                                           └──────┬──────┘
       │                                                         │
       │  1. After order delivery                                │
       │     GET /api/rewards/points                            │
       ├────────────────────────────────────────────────────>│
       │  { totalPoints, availablePoints, tier }          │
       │<────────────────────────────────────────────────────┤
       │  Earned points from purchase                     │
       │                                                         │
       │  2. Check reward history                                │
       │     GET /api/rewards/history                           │
       ├────────────────────────────────────────────────────>│
       │  { history of earned/redeemed points }          │
       │<────────────────────────────────────────────────────┤
       │                                                         │
       │  3. Browse reward catalog                               │
       │     GET /api/rewards/catalog                           │
       ├────────────────────────────────────────────────────>│
       │  { available rewards with point costs }         │
       │<────────────────────────────────────────────────────┤
       │                                                         │
       │  4. Redeem reward                                       │
       │     POST /api/rewards/redeem/:rewardId                 │
       ├────────────────────────────────────────────────────>│
       │                                                    │ Check points
       │                                                    │ Deduct points
       │                                                    │ Create voucher
       │  201 Created                                       │
       │  { rewardId, pointsUsed, voucher }               │
       │<────────────────────────────────────────────────────┤
       │                                                         │
       │  5. Use redeemed voucher in next purchase               │
       │     POST /api/cart/apply-voucher                       │
       │     { voucherCode: "REWARD_123" }                     │
       ├────────────────────────────────────────────────────>│
       │  { discountAmount, newTotal }                    │
       │<────────────────────────────────────────────────────┤
       │                                                         │
```

---

## 🔟 ADMIN DASHBOARD & ANALYTICS FLOW

```
┌──────────────────┐                        ┌──────────────────┐
│  ADMIN (Client)  │                        │  SERVER (Backend)│
└────────┬─────────┘                        └────────┬─────────┘
         │                                           │
         │  1. Dashboard Stats                       │
         │  GET /api/users/dashboard/stats           │
         ├──────────────────────────────────────────>│
         │  { totalRevenue, orderCount, growth }│
         │<──────────────────────────────────────────┤
         │                                           │
         │  2. Recent Orders                         │
         │  GET /api/users/dashboard/recent-orders   │
         ├──────────────────────────────────────────>│
         │  { recent 10 orders }                 │
         │<──────────────────────────────────────────┤
         │                                           │
         │  3. User Statistics                       │
         │  GET /api/users/stats                     │
         ├──────────────────────────────────────────>│
         │  { totalUsers, activeUsers, newThisMonth }
         │<──────────────────────────────────────────┤
         │                                           │
         │  4. All Orders Management                 │
         │  GET /api/orders?page=1&limit=10          │
         ├──────────────────────────────────────────>│
         │  { paginated orders list }            │
         │<──────────────────────────────────────────┤
         │                                           │
         │  5. Update Order Status                   │
         │  PATCH /api/orders/:id/status             │
         │  { status: "CONFIRMED" }                  │
         ├──────────────────────────────────────────>│
         │  { updated order }                    │
         │<──────────────────────────────────────────┤
         │                                           │
         │  6. Manage Products                       │
         │  GET /api/products                        │
         │  POST /api/products                       │
         │  PATCH /api/products/:id                  │
         │  DELETE /api/products/:id                 │
         ├──────────────────────────────────────────>│
         │  { CRUD operations }                  │
         │<──────────────────────────────────────────┤
         │                                           │
```

---

## 📊 Complete API Call Count by Feature

| Feature | GET | POST | PATCH | DELETE | Total |
|---------|-----|------|-------|--------|-------|
| Auth | 2 | 2 | 2 | 0 | 6 |
| Products | 8 | 1 | 1 | 1 | 11 |
| Categories | 3 | 1 | 1 | 1 | 6 |
| Cart | 2 | 2 | 1 | 2 | 7 |
| Orders | 3 | 1 | 4 | 0 | 8 |
| Payments | 2 | 2 | 1 | 1 | 6 |
| Shipments | 2 | 2 | 0 | 0 | 4 |
| Reviews | 3 | 1 | 1 | 2 | 7 |
| Designs | 3 | 2 | 1 | 1 | 7 |
| Addresses | 3 | 1 | 2 | 1 | 7 |
| Vouchers & Rewards | 4 | 1 | 0 | 0 | 5 |
| Users | 4 | 1 | 3 | 1 | 9 |
| Catalogs | 6 | 3 | 3 | 3 | 15 |
| Inventory | 5 | 2 | 0 | 0 | 7 |
| **TOTAL** | **51** | **22** | **20** | **13** | **106+** |

---

## 🎯 Common User Journeys

### Journey 1: First-Time Buyer
```
1. Register → Login
2. Browse categories & products
3. View product details
4. Add to cart
5. Checkout
6. Pay with VNPay
7. View order status
8. Receive package
9. Leave review
10. Earn reward points
```

### Journey 2: Design Customizer
```
1. Browse products
2. Open customizer
3. Design product (add text, images)
4. Calculate price
5. Save design for later
6. Add to cart with saved design
7. Checkout & pay
```

### Journey 3: Returning Customer
```
1. Auto login with stored token
2. Browse favorites
3. Add frequently bought items to cart
4. Apply voucher code
5. Quick checkout
6. Use same address
7. Quick payment
```

### Journey 4: Admin Operations
```
1. Login as admin
2. View dashboard stats
3. Manage pending orders
4. Update order status
5. Manage products & inventory
6. View customer reviews
7. Generate reports
```





